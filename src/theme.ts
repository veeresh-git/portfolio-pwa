import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#2C5F2D',
        },
        secondary: {
            main: '#97BC62',
        }
     }        
});

export default theme;